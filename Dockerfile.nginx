FROM nginx:alpine

# Копируем полную конфигурацию nginx (это главный nginx.conf, а не site config)
COPY nginx-server.conf /etc/nginx/nginx.conf

# Создаем директорию для логов
RUN mkdir -p /var/log/nginx

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
